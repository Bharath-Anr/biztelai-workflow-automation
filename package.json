{
  "name": "biztelai-workflow-automation",
  "version": "1.0.0",
  "description": "AI-Powered Workflow Automation System for handwritten operational documents",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm start --prefix server"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true
}
